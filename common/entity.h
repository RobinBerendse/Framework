#pragma once;
#ifndef ENTITY_H
#define ENTITY_H

#include<vector>


#include <common/entity.h>
#include <common/sprite.h>

class Entity
{
protected:
	Entity(void);
	~Entity(void);
public:

	
	//void setFileName(char* fileName) { m_sprite.Initialize(fileName); }
	
private:

	//void InitializSprite(ID3D11Device* device)
};

#endif
